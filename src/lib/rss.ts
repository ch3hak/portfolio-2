type Posts = {
  title: string;
  id: string;
  link: string;
  description: string;
  date: Date;
};

export const generateRssFeed = (): Array<Posts> => {
  const posts: Array<Posts> = [];

  posts.push();

  return posts;
};
